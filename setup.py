from setuptools import setup

setup(
    name="RoutingInfo",
    version="0.0.1",
    description="Accessing arbitrary routing information",
    author="Daisuke Kotani",
    install_requires=["mrtparse", "py-radix"],
)